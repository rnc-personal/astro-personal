---
const { layout } = Astro.props;
---

{
  layout === "vertical" ? (
    <div class=" bg-slate-600 py-28 px-1 w-1 opacity-5" />
  ) : (
    <section class="container xl:px96 lg:px-48 md:24px sm:px-8 px-6 my-12">
      <div class="bg-slate-600 py-1 px-28 opacity-5" />
    </section>
  )
}
